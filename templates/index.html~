<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="https://bootswatch.com/Paper/bootstrap.css" integrity="2hfp1SzUoho7/TsGGGDaFdsuuDL0LX2hnUp6VkX3CUQ2K4K+xjboZdsXyp4oUHZj" crossorigin="anonymous">
    </head>
    <body>
        <div class="jumbotron">
            <div class="container">
                {% if name %}
                    <div style = "margin-bottom:10px;" class="page-header">
                    <h1>Hi there {{ name }}!</h1>
                {% else %}
                    <div style= "margin-bottom:10px;" class= "page-header">
                    <h1>Hello! Thanks for visiting my site. I'd like to give you information about local places of interest such as dining and shopping options</h1>
                    <form action="/" class="form-inline">
                        <div class="form-group">
                            <input type="text" name="name" placeholder="Enter your name here" class="form-control">
                        </div>
                        <input type="submit" value="Submit" class="btn btn-primary">
                    </form>
                {% endif %}
                <a class="btn btn-secondary" href="{{ url_for('index') }}" role="button">Home »</a>
                <a class="btn btn-secondary" href="{{ url_for('about') }}" role="button">About »</a>
                <a class="btn btn-secondary" href="{{ url_for('weather') }}" role="button">Find Weather »</a>
                <a class="btn btn-secondary" href="{{ url_for('yelp') }}" role="button">Lookup Businesses »</a>
            </div>
        </div>
    </body>
</html>
